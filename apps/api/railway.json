{
  "build": {
    "builder": "NIXPACKS",
    "watchPatterns": [
      "src/**",
      "prisma/**",
      "package.json"
    ]
  },
  "deploy": {
    "startCommand": "npm run start:prod",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
